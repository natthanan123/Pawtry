# Pawtry

prompt Database Design: "Act as an Expert Database Architect. Design a lean MySQL relational database schema for a Task Management System. The schema must include 4 tables: 'users', 'categories', 'statuses', and 'tasks'. Exclude unnecessary fields like timestamps (created_at, updated_at), emails, and priority to keep the CRUD operations simple for a coding test. Keep essential foreign keys (category_id, status_id, created_by, assigned_to) in the 'tasks' table. Provide the exact SQL CREATE TABLE script with some mock data."


prompt AI-Assisted API: "Act as an Expert Node.js Developer. Create a RESTful API using Express.js for the Task Management System. Implement a POST endpoint to create a new task. The core requirement is to strictly validate the incoming JSON payload using 'Zod' based on our database schema (title, description, category_id, status_id, created_by, assigned_to). Provide the Express app code, and also use AI to write one Jest unit test suite (using supertest) to verify that the Zod validation works correctly for both success and failure cases."